#!/usr/bin/env bash
# yrxlsim — Bash wrapper; runs the Node CLI using the shared lib/yrxlsim.js core.
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$SCRIPT_DIR/yrxlsim.js" "$@"
